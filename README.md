# weynas-server
Home server for gaming and experiments.

## Ansible playbook
The IAC ansible tool allows to run and made configurations from files named 'playbooks'. To run the existing plabooks, change to `iac/` directory and execute the following command:

```bash
$ ansible-playbook -i inventory.yml main.yml
```
